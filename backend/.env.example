# Server Configuration
PORT=3000
NODE_ENV=development

# Authentication
JWT_SECRET=your_jwt_secret_key_here

# Database Configuration (SQLite for dev, easily replaceable with Postgres/MySQL)
DB_PATH=./data/xenobiz.db
